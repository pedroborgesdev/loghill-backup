import { AlertTriangle, MailCheck, Plus, RefreshCw, ShieldAlert, Zap } from "lucide-react";
import { useCallback, useEffect, useRef, useState } from "react";
import { useSearchParams } from "react-router-dom";
import { eventsApi } from "../api/events";
import { Listbox } from "../components/controls";
import { EventDetailsDrawer } from "../components/events/EventDetailsDrawer";
import { EventFormDialog } from "../components/events/EventFormDialog";
import { EventTable } from "../components/events/EventTable";
import { EventTestDialog } from "../components/events/EventTestDialog";
import { Button, ConfirmDialog, ErrorAlert, Input, MetricCard, Pagination, Panel, SearchInput } from "../components/ui";
import { useDebounce } from "../hooks/useDebounce";
import { useAppShell } from "../layouts/appShellContext";
import type { EventDefinition, EventPage } from "../types/event";
import { formatNumber } from "../utils/format";

export function EventsPage() {
  const [params, setParams] = useSearchParams(); const { refreshToken, setRefreshing, setStreamState, openEmailSettings } = useAppShell();
  const [data, setData] = useState<EventPage>(); const dataRef = useRef<EventPage | undefined>(undefined); const [initialLoading, setInitialLoading] = useState(true); const [loading, setLoading] = useState(false);
  const [error, setError] = useState(""); const [notice, setNotice] = useState(""); const [search, setSearch] = useState(params.get("search") ?? ""); const [senderFilter, setSenderFilter] = useState(params.get("sender_id") ?? "");
  const debouncedSearch = useDebounce(search); const debouncedSender = useDebounce(senderFilter); const [editing, setEditing] = useState<EventDefinition | "new">(); const [details, setDetails] = useState<EventDefinition>(); const [testing, setTesting] = useState<EventDefinition>(); const [deleting, setDeleting] = useState<EventDefinition>(); const [busyId, setBusyId] = useState("");
  const page = Math.max(1, Number(params.get("page") ?? 1)); const pageSize = Math.max(1, Number(params.get("page_size") ?? 20)); const enabled = params.get("enabled") ?? "all";
  useEffect(() => setStreamState(null), [setStreamState]);
  const load = useCallback(async () => { const hasPrevious = Boolean(dataRef.current); setLoading(true); setRefreshing(hasPrevious); try { const query = new URLSearchParams({ page: String(page), page_size: String(pageSize) }); if (debouncedSearch) query.set("search", debouncedSearch); if (debouncedSender) query.set("sender_id", debouncedSender); if (enabled !== "all") query.set("enabled", enabled); const next = await eventsApi.list(query.toString()); dataRef.current = next; setData(next); setError(""); } catch (requestError) { setError(requestError instanceof Error ? requestError.message : "Não foi possível carregar os eventos."); } finally { setLoading(false); setInitialLoading(false); setRefreshing(false); } }, [debouncedSearch, debouncedSender, enabled, page, pageSize, setRefreshing]);
  useEffect(() => { void load(); }, [load, refreshToken]);
  useEffect(() => {
    setParams((current) => {
      const next = new URLSearchParams(current);
      if (debouncedSearch) next.set("search", debouncedSearch); else next.delete("search");
      if (debouncedSender) next.set("sender_id", debouncedSender); else next.delete("sender_id");
      next.set("page", "1");
      return next.toString() === current.toString() ? current : next;
    }, { replace: true });
  }, [debouncedSearch, debouncedSender, setParams]);
  const updateFilter = (key: string, value: string) => { const next = new URLSearchParams(params); if (value && value !== "all") next.set(key, value); else next.delete(key); next.set("page", "1"); setParams(next); };
  const toggle = async (event: EventDefinition) => { setBusyId(event.id); setNotice(""); try { await eventsApi.setEnabled(event.id, !event.enabled); setNotice(event.enabled ? "Evento desativado." : "Evento ativado."); await load(); } catch (requestError) { setNotice(requestError instanceof Error ? requestError.message : "Não foi possível alterar o evento."); } finally { setBusyId(""); } };
  const remove = async () => { if (!deleting) return; setBusyId(deleting.id); try { await eventsApi.remove(deleting.id); setDeleting(undefined); setNotice("Evento excluído."); await load(); } catch (requestError) { setNotice(requestError instanceof Error ? requestError.message : "Não foi possível excluir o evento."); } finally { setBusyId(""); } };
  const outlookReady = Boolean(data?.emailProvider.configured && data.emailProvider.enabled); const summary = data?.summary ?? { total: 0, active: 0, recentTriggered: 0, recentFailures: 0 };
  return <div className="space-y-4"><div className="flex flex-wrap items-end justify-between gap-3"><div><h2 className="text-xl font-semibold">Eventos</h2><p className="mt-1 text-sm text-zinc-500">Execute ações quando um log informar explicitamente uma chave de evento.</p></div><Button onClick={() => setEditing("new")}><Plus className="size-4" />Novo evento</Button></div>{!outlookReady && data && <div className="flex flex-wrap items-center gap-3 rounded-xl border border-amber-950 bg-amber-950/15 px-4 py-3"><AlertTriangle className="size-4 text-amber-500" /><div className="min-w-0 flex-1"><p className="text-xs font-medium text-amber-300">Outlook não configurado</p><p className="text-[11px] text-zinc-500">Eventos podem ser salvos como inativos, mas só poderão ser ativados após configurar o provider.</p></div><Button onClick={(click) => openEmailSettings(click.currentTarget)} className="h-8">Configurar Outlook</Button></div>}{outlookReady && <div className="flex items-center gap-2 rounded-lg border border-emerald-950 bg-emerald-950/15 px-3 py-2 text-xs text-emerald-400"><MailCheck className="size-4" />Outlook pronto para executar eventos de e-mail.</div>}<div className="grid gap-3 sm:grid-cols-2 xl:grid-cols-4"><MetricCard label="Eventos" value={formatNumber(summary.total)} hint="configurados" icon={<Zap className="size-4" />} loading={initialLoading} /><MetricCard label="Ativos" value={formatNumber(summary.active)} hint="aceitando disparos" icon={<MailCheck className="size-4 text-emerald-500" />} loading={initialLoading} /><MetricCard label="Disparados recentemente" value={formatNumber(summary.recentTriggered)} hint="nas últimas 24 horas" icon={<Zap className="size-4 text-amber-500" />} loading={initialLoading} /><MetricCard label="Falhas recentes" value={formatNumber(summary.recentFailures)} hint="nas últimas 24 horas" icon={<ShieldAlert className="size-4 text-red-500" />} loading={initialLoading} /></div><Panel className="overflow-hidden"><div className="border-b border-zinc-800 p-3"><div className="flex flex-col gap-2 xl:flex-row xl:items-center"><div className="min-w-0 flex-1"><div className="flex items-center gap-2"><h3 className="text-sm font-medium text-zinc-200">Eventos configurados</h3>{loading && data && <RefreshCw className="size-3 animate-spin text-zinc-600" />}</div><p className="text-[11px] text-zinc-600">O matching usa a chave exata e os senders associados, independentemente da severity.</p></div><div className="flex flex-col gap-2 sm:flex-row"><SearchInput value={search} onChange={setSearch} placeholder="Buscar nome ou chave" className="w-full sm:w-64" /><label className="sr-only" htmlFor="event-sender-filter">Filtrar por sender</label><Input id="event-sender-filter" value={senderFilter} onChange={(change) => setSenderFilter(change.target.value)} placeholder="ID do sender" className="w-full sm:w-48" /><Listbox value={enabled} onChange={(value) => updateFilter("enabled", value)} label="Status do evento" className="w-full sm:w-36" options={[{ value: "all", label: "Todos" }, { value: "true", label: "Ativos" }, { value: "false", label: "Inativos" }]} /><Button onClick={() => void load()} disabled={loading}><RefreshCw className={`size-4 ${loading ? "animate-spin" : ""}`} />Atualizar</Button></div></div></div>{error && <div className="p-3"><ErrorAlert message={error} onRetry={() => void load()} /></div>}{notice && <p role="status" className="border-b border-zinc-800 px-4 py-2 text-xs text-zinc-300">{notice}</p>}{initialLoading && !data ? <div className="grid min-h-60 place-items-center text-xs text-zinc-600">Carregando eventos...</div> : data?.items.length ? <div className={loading ? "opacity-70 transition-opacity" : ""}><EventTable items={data.items} busyId={busyId} onDetails={setDetails} onEdit={setEditing} onToggle={(event) => void toggle(event)} onTest={setTesting} onDelete={setDeleting} /></div> : <div className="grid min-h-64 place-items-center p-6 text-center"><div><Zap className="mx-auto size-7 text-zinc-700" /><p className="mt-3 text-sm font-medium text-zinc-300">Nenhum evento configurado</p><p className="mt-1 max-w-sm text-xs leading-5 text-zinc-600">Crie uma ação explícita para ser chamada pelo parâmetro <code>event</code> dos seus logs.</p><Button onClick={() => setEditing("new")} className="mt-4"><Plus className="size-4" />Novo evento</Button></div></div>}<Pagination page={data?.pagination.page ?? page} totalPages={data?.pagination.total_pages ?? 1} total={data?.pagination.total ?? 0} pageSize={pageSize} busy={loading} onPageSizeChange={(value) => { const next = new URLSearchParams(params); next.set("page_size", String(value)); next.set("page", "1"); setParams(next); }} onChange={(value) => { const next = new URLSearchParams(params); next.set("page", String(value)); setParams(next); }} /></Panel>{editing && <EventFormDialog event={editing === "new" ? undefined : editing} outlookReady={outlookReady} onClose={() => setEditing(undefined)} onConfigureOutlook={(trigger) => { setEditing(undefined); openEmailSettings(trigger); }} onSaved={() => { setEditing(undefined); setNotice("Evento salvo com sucesso."); void load(); }} />}<EventDetailsDrawer event={details} onClose={() => setDetails(undefined)} onEdit={(event) => { setDetails(undefined); setEditing(event); }} onTest={(event) => setTesting(event)} /><EventTestDialog event={testing} onClose={() => setTesting(undefined)} onSent={() => setNotice("E-mail de teste enfileirado.")} /><ConfirmDialog open={Boolean(deleting)} title="Excluir evento?" description={deleting ? `O evento “${deleting.name}” e a chave ${deleting.key} serão removidos permanentemente.` : ""} confirmLabel="Excluir evento" onClose={() => setDeleting(undefined)} onConfirm={() => void remove()} /></div>;
}
